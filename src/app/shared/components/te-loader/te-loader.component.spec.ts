import { TeLoaderComponent } from './te-loader.component';

describe('TeLoaderComponent', () => {
  let component: TeLoaderComponent;

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
