import { IfPermittedForDirective } from './if-permitted-for.directive';

describe('IfPermittedForDirective', () => {
  it('should create an instance', () => {
    const directive = new IfPermittedForDirective();
    expect(directive).toBeTruthy();
  });
});
